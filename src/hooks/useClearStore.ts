// import {useAppDispatch} from './hooks';

export default function useClearStore() {
  // const dispatch = useAppDispatch();
  const clearStore = () => {
    // clear slice
  };

  return {clearStore};
}
